{
  "name": "ridebuddy-website",
  "private": true,
  "type": "module",
  "description": "Public site for ridebuddy.co.in. Root is a holding page, /estimate is the fare estimator, /beta serves the design-canvas artboard from ../Webpage; worker/ saves the site's two forms to Cloudflare D1. Its own npm project — the monorepo is deliberately not a workspace.",
  "scripts": {
    "build": "node build.mjs",
    "test": "node test/parity.mjs",
    "test:worker": "node test/worker.mjs",
    "preview": "node build.mjs && cd .. && npx --yes wrangler@4 dev --port 8099"
  },
  "devDependencies": {
    "@babel/standalone": "7.29.0",
    "esbuild": "0.24.2",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "sharp": "^0.33.5"
  }
}
