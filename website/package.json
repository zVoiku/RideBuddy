{
  "name": "ridebuddy-website",
  "private": true,
  "type": "module",
  "description": "Public site for ridebuddy.co.in. Root is a holding page; /beta serves the design-canvas artboard from ../Webpage. Its own npm project — the monorepo is deliberately not a workspace.",
  "scripts": {
    "build": "node build.mjs",
    "preview": "node build.mjs && npx --yes http-server dist -p 8099 -c-1"
  },
  "devDependencies": {
    "@babel/standalone": "7.29.0",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "sharp": "^0.33.5"
  }
}
