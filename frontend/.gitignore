# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# Native projects — regenerated by `expo prebuild` / `expo run:ios`. Committing
# them would drop a few thousand generated files (and Pods) into the tree, and
# they carry machine-specific signing settings.
ios/
android/

# Metro's on-disk bundler cache. NOTE: ~23k of these are already tracked from
# earlier auto-commits; this only stops new ones. Untracking the rest needs
# `git rm -r --cached frontend/.metro-cache`, left as a deliberate decision
# because it is a very large, unrelated diff.
.metro-cache/

# macOS
.DS_Store
*.pem

# local env files
.env*.local

# typescript
*.tsbuildinfo

app-example
